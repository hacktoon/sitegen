y = 0;

function sum(a, b){
    yy = 0;
    return a+b;
}

while y < 10 {
    if y == 5 {
        a = "cinco";
        c = 'doi"s';
        b = sum(y, y*2);
    }
    y = y + 1;
}

bool = true and false;
print 'Informe um valor:';
read x;
print x;
